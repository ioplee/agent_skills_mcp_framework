apiVersion: v1
kind: Secret
metadata:
  name: {{ include "agent-skill-mcp.fullname" . }}-secrets
  labels:
    {{- include "agent-skill-mcp.labels" . | nindent 4 }}
type: Opaque
data:
  OPENAI_API_KEY: {{ .Values.secrets.openaiApiKey | b64enc }}
  OPENAI_API_BASE: {{ .Values.secrets.openaiApiBase | b64enc }}
