apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-skill-mcp.fullname" . }}
  labels:
    {{- include "agent-skill-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent-skill-mcp.selectorLabels" . | nindent 4 }}
