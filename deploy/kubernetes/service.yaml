apiVersion: v1
kind: Service
metadata:
  name: agent-skill-mcp
  labels:
    app: agent-skill-mcp
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: agent-skill-mcp
