apiVersion: v1
kind: Secret
metadata:
  name: agent-skill-mcp-secrets
type: Opaque
data:
  # 注意：这些值需要使用base64编码
  # 例如：echo -n "your-openai-api-key" | base64
  OPENAI_API_KEY: "your-openai-api-key-base64-encoded"
  OPENAI_API_BASE: "aHR0cHM6Ly9hcGkub3BlbmFpLmNvbS92MQ==" # https://api.openai.com/v1
